{% extends 'base.html.twig'  %}

{% block body %}
    <div>
        <h1>The current message is</h1>
        <div>
            <b>{{ message.message }}</b>
            {{ include('hello/_message.html.twig', {message: message}) }}
        </div>
    </div>
{% endblock %}

{% block title %}
    {{ message.message }}
{% endblock %}